export class Child implements Object{
  constructor(
    public familyId: number,
    public firstName: string,
    public secondName: string,
    public pesel: string,
    public sex: string
  ) { }
}
